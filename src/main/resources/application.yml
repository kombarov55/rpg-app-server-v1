server:
  port: 8080
#  ssl:
#    key-store-type: PKCS12
#    key-store: classpath:keystore.p12
#    key-store-password: Qhxmfa49
#    key-alias: tomcat

spring:
  codec:
    max-in-memory-size: 10MB
  datasource:
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 10
    url: jdbc:h2:file:/var/h2/rpg
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true

imgPrefix:  "http://localhost:8081"
hidden-props-path: /Users/u18456857/IdeaProjects/rpg-app-server-v1/src/main/resources/hidden.properties

vk:
  api-version: 5.103

jwt:
  secret: Satan